[Unit]
Description=Loki service
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/loki-linux-amd64 -config.file={{ loki_config_file }} -log.level={{ loki_log_level }}

[Install]
WantedBy=multi-user.target